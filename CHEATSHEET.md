# 📝 Шпаргалка команд

## Быстрый старт (Windows PowerShell)

### 1. Установка PostgreSQL
```powershell
# Скачайте и установите: https://www.postgresql.org/download/windows/
# При установке запомните пароль для пользователя postgres
```

### 2. Создание базы данных
```sql
-- Откройте pgAdmin или psql и выполните:
CREATE DATABASE construction_db;
```

### 3. Настройка проекта
```powershell
# Перейдите в папку проекта
cd "c:\Users\LOQ\Desktop\App\ГПР MBC"

# Обновите .env файл (замените password на ваш пароль PostgreSQL)
# DATABASE_URL=postgresql://postgres:ВАШ_ПАРОЛЬ@localhost:5432/construction_db

# Создайте таблицы в БД
npm run db:migrate
```

### 4. Запуск приложения
```powershell
# Запустить backend + frontend одновременно
npm run dev

# Или по отдельности:
npm run server   # Только backend (порт 3000)
npm run client   # Только frontend (порт 5173)
```

### 5. Тестирование
```
Откройте браузер: http://localhost:5173
```

## Команды для работы

```powershell
# Установка зависимостей (если еще не установлены)
npm install
cd client ; npm install ; cd ..

# Создание/пересоздание таблиц БД
npm run db:migrate

# Запуск в режиме разработки
npm run dev

# Сборка для production
npm run build

# Запуск production версии
npm start
```

## Git команды для GitHub

```powershell
# Инициализация репозитория
git init

# Добавить все файлы
git add .

# Первый коммит
git commit -m "Initial commit - MVP система учета работ"

# Подключить удаленный репозиторий (замените URL на свой)
git remote add origin https://github.com/ВАШ_АККАУНТ/construction-tracking.git

# Отправить на GitHub
git push -u origin main
```

## Railway деплой

```powershell
# После пуша на GitHub:

1. Зайдите на https://railway.app
2. New Project → Deploy from GitHub repo
3. Выберите ваш репозиторий
4. New → Database → Add PostgreSQL
5. Settings → Variables → добавьте:
   - JWT_SECRET=ваш-секретный-ключ
   - NODE_ENV=production
   - PORT=3000
6. Settings → Generate Domain
7. Готово! Приложение доступно онлайн
```

## Тестовые пользователи

После запуска зарегистрируйте:

| Роль | Username | Password | Компания |
|------|----------|----------|----------|
| Плановик | planner1 | 123456 | Отдел планирования |
| Прораб | foreman1 | 123456 | ГенПодряд |
| Субподрядчик | sub1 | 123456 | СтройАльянс ООО |
| Субподрядчик | sub2 | 123456 | МонтажСервис ООО |

## Тестовый сценарий

```
1. Плановик → Загрузить "Объект 1.xml"
2. Прораб → Выбрать объект → Сформировать список работ
3. Прораб → Распределить работу → Ввести ID субподрядчика и объем
4. Субподрядчик → Сдать работу → Ввести выполненный объем
5. Прораб → Входящие → Подтвердить выполнение
6. Плановик → Экспорт XML → Скачать обновленный файл
```

## Полезные URL

```
Frontend (dev):       http://localhost:5173
Backend (dev):        http://localhost:3000
API Health:           http://localhost:3000/api/health
PostgreSQL (local):   localhost:5432
pgAdmin:              http://localhost/pgadmin (если установлен)
Railway Dashboard:    https://railway.app/dashboard
```

## Структура API

### Auth
- POST `/api/auth/register` - Регистрация
- POST `/api/auth/login` - Вход
- GET `/api/auth/users?role=subcontractor` - Список пользователей

### Planner
- POST `/api/planner/upload` - Загрузка XML (multipart/form-data)
- GET `/api/planner/objects` - Список объектов
- GET `/api/planner/objects/:id` - Детали объекта
- GET `/api/planner/export/:objectId` - Экспорт XML

### Foreman
- GET `/api/foreman/upcoming-works/:objectId?weeks=2` - Ближайшие работы
- POST `/api/foreman/assign-work` - Распределить работу
- GET `/api/foreman/pending-approvals/:foremanId` - Ожидающие проверки
- POST `/api/foreman/approve-work` - Подтвердить/отклонить

### Subcontractor
- GET `/api/subcontractor/my-assignments/:subId` - Мои наряды
- POST `/api/subcontractor/submit-work` - Сдать работу
- GET `/api/subcontractor/statistics/:subId` - Статистика
- GET `/api/subcontractor/work-history/:subId` - История работ

## Troubleshooting

### Ошибка подключения к БД
```powershell
# Проверьте, что PostgreSQL запущен
# Проверьте .env файл
# Проверьте пароль в DATABASE_URL
```

### Порт уже используется
```powershell
# Найдите процесс на порту 3000
netstat -ano | findstr :3000

# Убейте процесс (замените PID)
taskkill /PID <номер_процесса> /F
```

### Таблицы не созданы
```powershell
npm run db:migrate
```

### Frontend не видит Backend
```powershell
# Убедитесь что backend запущен на порту 3000
# Vite proxy настроен автоматически в vite.config.js
```

## Файлы проекта

```
📁 Главные файлы:
├── START_HERE.md     ⭐ Начните отсюда!
├── QUICKSTART.md     📖 Пошаговое руководство
├── DEPLOY.md         🚀 Инструкции по деплою
├── README.md         📚 Общая документация
└── package.json      📦 Конфигурация проекта

📁 Backend:
├── server/index.js        # Entry point
├── server/routes/         # API endpoints
├── server/db/             # База данных
└── server/utils/          # Утилиты (XML парсер)

📁 Frontend:
├── client/src/App.jsx     # Главный компонент
├── client/src/pages/      # Страницы по ролям
├── client/src/api.js      # API клиент
└── client/vite.config.js  # Vite конфигурация

📁 Deploy:
├── Dockerfile             # Docker конфигурация
└── railway.json           # Railway конфигурация
```

## Быстрые ссылки

- PostgreSQL: https://www.postgresql.org/download/
- Railway: https://railway.app
- GitHub: https://github.com
- Node.js: https://nodejs.org (уже установлен)

---

💡 **Совет:** Держите этот файл открытым во время работы!
