# Обновление архитектуры: 3-уровневая иерархия

## Дата: 3 декабря 2024

### Изменения

Система была полностью переработана с **плоской структуры** (Объект → XML) на **3-уровневую иерархию**:

```
Объект → Секции → XML файлы
```

---

## Новая структура базы данных

### 1. Таблица `objects`
- `id` - уникальный идентификатор
- `name` - название объекта
- `created_by` - кем создан
- `created_at` - дата создания
- `updated_at` - дата обновления

### 2. Таблица `sections`
- `id` - уникальный идентификатор
- `object_id` - FK на objects
- `section_number` - номер секции (1, 2, 3...)
- `section_name` - название секции
- `created_by` - кем создана
- `created_at` - дата создания
- **UNIQUE** (`object_id`, `section_number`) - уникальность номера внутри объекта

### 3. Таблица `xml_files`
- `id` - уникальный идентификатор
- `section_id` - FK на sections
- `filename` - название файла
- `filepath` - путь к файлу
- `file_size` - размер в байтах
- `uploaded_by` - кто загрузил
- `uploaded_at` - дата загрузки
- `status` - статус ('active', 'replaced', 'deleted')

### 4. Таблица `work_items`
**Изменено**: теперь привязана к секции вместо объекта
- `section_id` - FK на sections (было `object_id`)
- Остальные поля без изменений

---

## Workflow плановика

### 1. Создание объекта
```
POST /planner/objects
Body: { name: "Название объекта" }
```

### 2. Создание секций
```
POST /planner/objects/:objectId/sections
Body: { section_number: 1, section_name: "Секция №1" }
```

### 3. Загрузка XML файлов
```
POST /planner/sections/:sectionId/upload-xml
FormData: { file: xmlFile }
```

**Система автоматически фиксирует:**
- № (порядковый номер)
- Дата и время добавления
- Название файла
- Размер файла
- Кем добавлен
- Статус

### 4. Замена файлов
При загрузке нового XML:
- Старый файл получает статус `replaced`
- Новый файл получает статус `active`
- История сохраняется

---

## Workflow прораба

### 1. Выбор объекта и секции
```jsx
<select> Объект </select>
<select> Секция </select>
```

### 2. Загрузка работ
**Без генерации!** Система автоматически загружает работы из активного XML файла секции:
```
GET /foreman/sections/:sectionId/works?weeksAhead=2
```

### 3. Проверка обновлений
Кнопка **"🔄 Обновить"** подсвечивается оранжевым, если плановик загрузил новый XML:
```
GET /foreman/sections/:sectionId/check-updates?lastChecked=2024-12-03T10:00:00
Response: { hasUpdates: true, lastUpload: "2024-12-03T11:30:00" }
```

### 4. Распределение работ
Без изменений - прораб назначает работы субподрядчикам.

---

## Workflow субподрядчика

Без изменений - получает наряды и отчитывается о выполнении.

**Отображение:**
- Карточки теперь показывают **"Объект / Секция"**
- Таблица показывает название объекта и секции

---

## Новые файлы

### Backend
- `server/routes/planner-new.js` - полностью новый API плановика
- `server/routes/foreman-new.js` - обновленный API прораба

### Frontend
- `client/src/pages/PlannerPageNew.jsx` - новый UI плановика (3 колонки)
- `client/src/pages/ForemanPageNew.jsx` - новый UI прораба с объектами/секциями

### Обновлены
- `server/db/migrate.js` - миграция схемы БД
- `client/src/api.js` - новые методы API
- `client/src/App.jsx` - подключение новых компонентов
- `client/src/components/KanbanBoard.jsx` - отображение объекта/секции
- `client/src/pages/SubcontractorPage.jsx` - отображение объекта/секции
- `server/routes/subcontractor.js` - JOIN с sections и objects

---

## Преимущества новой архитектуры

1. **Масштабируемость**: можно управлять сотнями секций независимо
2. **Версионирование**: полная история XML файлов с статусами
3. **Метаданные**: автоматическая фиксация размера, даты, автора
4. **Обнаружение обновлений**: прораб видит, когда плановик загрузил новый XML
5. **Независимость секций**: работы одной секции не мешают другим
6. **Audit trail**: кто, когда и что загружал - полная история

---

## Миграция

База данных автоматически мигрируется при запуске сервера через `migrate.js`.

**Внимание:** Старые данные из таблицы `objects` (старая структура) не мигрируются автоматически. Начните с создания новых объектов.

---

## Deployment

После пуша в GitHub, Railway автоматически:
1. Соберет новую версию
2. Выполнит миграцию БД
3. Перезапустит сервер

**Проверьте:** логи Railway для подтверждения успешной миграции.

---

## Следующие шаги

- ✅ Плановик: создание объектов, секций, загрузка XML
- ✅ Прораб: выбор объекта/секции, обновление, распределение
- ✅ Субподрядчик: отображение объекта/секции
- ⏳ Тестирование полного цикла
- ⏳ Деплой на Railway

---

**Автор обновления:** GitHub Copilot  
**Commit:** aaaf460
