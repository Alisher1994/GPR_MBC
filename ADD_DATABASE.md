# ⚠️ ДОБАВЬТЕ POSTGRESQL!

Ваше приложение задеплоено, но ему нужна база данных PostgreSQL!

## 🔧 Что делать:

### 1️⃣ Добавьте PostgreSQL в Railway:
   - Откройте ваш проект в Railway
   - Нажмите **"New"** → **"Database"** → **"Add PostgreSQL"**
   - Подождите 30 секунд пока создастся

### 2️⃣ Добавьте переменные окружения:
   В разделе Variables добавьте:
   ```
   JWT_SECRET=construction-mvp-secret-2025
   NODE_ENV=production
   PORT=3000
   ```

### 3️⃣ Перезапустите деплой:
   - Railway автоматически перезапустит приложение
   - Или нажмите "Redeploy"

### 4️⃣ Проверьте:
   Откройте: `ваш-url.railway.app/api/health`
   
   Должно быть:
   ```json
   {
     "status": "ok",
     "database": "connected"
   }
   ```

## ✅ Готово!

Теперь можно:
- Зарегистрировать пользователей
- Загрузить XML файлы
- Работать с системой

---

📖 Подробная инструкция: **RAILWAY_SETUP.md**
