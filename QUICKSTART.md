# 🚀 Быстрый старт

## Шаг 1: Установка зависимостей

```powershell
# Основные зависимости
npm install

# Frontend зависимости
cd client
npm install
cd ..
```

## Шаг 2: Настройка базы данных

### Вариант A: Локальная PostgreSQL

1. Установите PostgreSQL с https://www.postgresql.org/download/windows/
2. Создайте базу данных:

```sql
CREATE DATABASE construction_db;
```

3. Создайте файл `.env`:

```bash
cp .env.example .env
```

4. Отредактируйте `.env`:

```
DATABASE_URL=postgresql://postgres:ваш_пароль@localhost:5432/construction_db
PORT=3000
JWT_SECRET=your-secret-key-change-in-production
NODE_ENV=development
```

### Вариант B: Railway (сразу онлайн)

Если хотите сразу деплоить онлайн, пропустите локальную настройку и следуйте `DEPLOY.md`

## Шаг 3: Создание таблиц

```powershell
npm run db:migrate
```

## Шаг 4: Запуск

```powershell
# Запустить backend и frontend одновременно
npm run dev
```

- Backend: http://localhost:3000
- Frontend: http://localhost:5173

## Шаг 5: Первый вход

1. Откройте http://localhost:5173
2. Нажмите "Нет аккаунта? Зарегистрироваться"
3. Создайте пользователей:

**Плановик:**
- Имя: planner1
- Пароль: 123456
- Роль: Плановик
- Компания: Отдел планирования

**Прораб:**
- Имя: foreman1
- Пароль: 123456
- Роль: Прораб
- Компания: ГенПодряд

**Субподрядчик 1:**
- Имя: sub1
- Пароль: 123456
- Роль: Субподрядчик
- Компания: СтройАльянс ООО

## Шаг 6: Тестирование потока

### 1. Войдите как Плановик

- Загрузите файл `Объект 1.xml`
- Система импортирует все работы
- Проверьте детали объекта

### 2. Войдите как Прораб

- Выберите объект
- Нажмите "Сформировать список работ"
- Выберите работу и нажмите "Распределить"
- Введите ID субподрядчика (например: 3) и объем (например: 100)

### 3. Войдите как Субподрядчик

- Увидите назначенную работу
- Нажмите "Сдать работу"
- Введите выполненный объем (например: 50)

### 4. Снова войдите как Прораб

- Перейдите на вкладку "Входящие"
- Увидите выполненный объем от субподрядчика
- Нажмите "Подтвердить"

### 5. Войдите как Плановик

- Нажмите "Экспорт XML" для объекта
- Получите обновленный файл с фактом выполнения

## Полезные команды

```powershell
# Только backend
npm run server

# Только frontend
npm run client

# Пересоздать таблицы БД
npm run db:migrate

# Проверка здоровья API
# Откройте http://localhost:3000/api/health
```

## Что дальше?

- 📖 Читайте `README.md` для полной документации
- 🚀 Следуйте `DEPLOY.md` для деплоя на Railway
- 🐛 При проблемах проверьте раздел Troubleshooting в `DEPLOY.md`

## Структура проекта

```
├── server/              # Backend (Express + PostgreSQL)
│   ├── index.js        # Entry point
│   ├── routes/         # API endpoints
│   │   ├── auth.js     # Аутентификация
│   │   ├── planner.js  # Функции плановика
│   │   ├── foreman.js  # Функции прораба
│   │   └── subcontractor.js  # Функции субподрядчика
│   ├── db/             # База данных
│   │   ├── pool.js     # Подключение
│   │   └── migrate.js  # Миграции
│   └── utils/          # Утилиты
│       └── xmlParser.js  # Парсинг XML
│
├── client/             # Frontend (React + Vite)
│   ├── src/
│   │   ├── pages/      # Страницы по ролям
│   │   │   ├── PlannerPage.jsx
│   │   │   ├── ForemanPage.jsx
│   │   │   └── SubcontractorPage.jsx
│   │   ├── components/ # Компоненты
│   │   │   └── Login.jsx
│   │   ├── api.js      # API клиент
│   │   ├── App.jsx     # Главный компонент
│   │   └── main.jsx    # Entry point
│   └── package.json
│
├── uploads/            # Загруженные XML (создается автоматически)
├── Dockerfile          # Docker конфигурация
├── railway.json        # Railway конфигурация
└── package.json        # Основной package.json
```

## Основные технологии

- **Backend:** Node.js, Express, PostgreSQL
- **Frontend:** React, Vite
- **Auth:** JWT, bcryptjs
- **XML:** xml2js
- **Deploy:** Railway, Docker

Удачи! 🎉
